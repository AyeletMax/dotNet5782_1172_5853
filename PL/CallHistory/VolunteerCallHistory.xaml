<!--גירסה בלי עיצוב--> 
<!--<Window x:Class="PL.CallHistory.VolunteerCallHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Volunteer Call History" Height="650" Width="1000"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Call Type:" VerticalAlignment="Center"/>
            <ComboBox Width="150" Margin="5,0"
                      ItemsSource="{Binding CallTypes}"
                      SelectedItem="{Binding SelectedCallType, Mode=TwoWay}"/>

            <Label Content="Sort By:" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <ComboBox Width="150" Margin="5,0"
                      ItemsSource="{Binding SortFields}"
                      SelectedItem="{Binding SelectedSortField, Mode=TwoWay}"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding ClosedCalls}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Call ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Call Type" Binding="{Binding CallType}"/>
                <DataGridTextColumn Header="Address" Binding="{Binding CallAddress}"/>
                <DataGridTextColumn Header="Opened" Binding="{Binding OpeningTime}"/>
                <DataGridTextColumn Header="Started" Binding="{Binding TreatmentStartTime}"/>
                <DataGridTextColumn Header="Ended" Binding="{Binding ActualTreatmentEndTime}"/>
                <DataGridTextColumn Header="Finish Type" Binding="{Binding FinishCallType}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>-->


<Window x:Class="PL.CallHistory.VolunteerCallHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.CallHistory"
        mc:Ignorable="d"
        Title="Volunteer Call History" Height="650" Width="1000"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F3F6FD" Offset="0.0"/>
            <GradientStop Color="#FDE4EC" Offset="1.0"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <!-- Style for Round Buttons -->
        <Style x:Key="RoundButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#5C6BC0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!-- Centered Column Headers -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#3F51B5"/>
        </Style>

        <!-- Centered TextBlock -->
        <Style x:Key="CenteredText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📜 Closed Calls You Handled" FontSize="24" FontWeight="Bold" Foreground="#3F51B5" HorizontalAlignment="Center" Margin="0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Text="Call Type:" FontSize="12" Margin="2"/>
                <ComboBox Width="150" Height="28"
                                  SelectedItem="{Binding SelectedCallType, Mode=TwoWay}" ItemsSource="{Binding CallTypes}">
                    <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontSize="12"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Text="Sort By:" FontSize="12" Margin="2"/>
           
                <ComboBox Width="150" Height="28"
                          SelectedItem="{Binding SelectedSortField, Mode=TwoWay}" ItemsSource="{Binding SortFields}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="12"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding ClosedCalls}"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  EnableRowVirtualization="True"
                  HorizontalAlignment="Center">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Call ID" Binding="{Binding Id}" ElementStyle="{StaticResource CenteredText}" Width="80"/>
                <DataGridTextColumn Header="Type" Binding="{Binding CallType}" ElementStyle="{StaticResource CenteredText}" Width="180"/>
                <DataGridTextColumn Header="Address" Binding="{Binding CallAddress}" Width="200"/>
                <DataGridTextColumn Header="Opened" Binding="{Binding OpeningTime}" ElementStyle="{StaticResource CenteredText}" Width="160"/>
                <DataGridTextColumn Header="Started" Binding="{Binding TreatmentStartTime}" ElementStyle="{StaticResource CenteredText}" Width="160"/>
                <DataGridTextColumn Header="Ended" Binding="{Binding ActualTreatmentEndTime}" ElementStyle="{StaticResource CenteredText}" Width="160"/>
                <DataGridTextColumn Header="Finish Type" Binding="{Binding FinishCallType}" ElementStyle="{StaticResource CenteredText}" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
