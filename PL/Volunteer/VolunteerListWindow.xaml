<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Volunteer List" Height="640" Width="900"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F3F6FD" Offset="0.0"/>
            <GradientStop Color="#FDE4EC" Offset="1.0"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <local:VolunteerSortFieldCollection x:Key="VolunteerSortFieldCollection"/>

        <!-- סגנון כפתור עגול -->
        <Style x:Key="RoundButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#5C6BC0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- סגנון DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!-- סגנון כותרות ממורכזות -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#3F51B5"/>
        </Style>

        <!-- עיצוב טקסט ממורכז -->
        <Style x:Key="CenteredText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- כותרת -->
        <TextBlock Grid.Row="0" Text="&#x1F3B5; Volunteer List for Music &amp; Singing &#x1F3B5;"
           FontSize="24" FontWeight="Bold" Foreground="#3F51B5"
           HorizontalAlignment="Center" Margin="0,10"/>

        <!-- מיון וסינון -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Text="Sort By:" FontSize="12" Margin="2"/>
                <ComboBox Width="150" Height="28"
                          SelectedValue="{Binding SelectedSortField, Mode=TwoWay}"
                          ItemsSource="{Binding Source={StaticResource VolunteerSortFieldCollection}}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="12"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Text="Filter by Call Type:" FontSize="12" Margin="2"/>
                <ComboBox Width="150" Height="28"
                          SelectedValue="{Binding SelectedCallType, Mode=TwoWay}"
                          ItemsSource="{Binding CallTypeList}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="12"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <!-- טבלת מתנדבים -->
        <DataGrid Grid.Row="2" Margin="0"
                  ItemsSource="{Binding VolunteerList}"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  EnableRowVirtualization="True"
                  RowDetailsVisibilityMode="VisibleWhenSelected"
                  SelectedItem="{Binding SelectedVolunteer}"
                  MouseDoubleClick="lsvVolunteerList_MouseDoubleClick"
                  HorizontalAlignment="Center">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="100" ElementStyle="{StaticResource CenteredText}"/>

                <DataGridTemplateColumn Header="Name" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="DarkBlue" TextAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Is Active" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Active}" IsEnabled="False" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Binding="{Binding TotalResponsesHandled}" Header="Total Responses Handled" Width="150" ElementStyle="{StaticResource CenteredText}"/>
                <DataGridTextColumn Binding="{Binding TotalResponsesCancelled}" Header="Total Responses Cancelled" Width="150" ElementStyle="{StaticResource CenteredText}"/>
                <DataGridTextColumn Binding="{Binding TotalExpiredResponses}" Header="Total Expired Responses" Width="150" ElementStyle="{StaticResource CenteredText}"/>
                <!--<DataGridTextColumn Binding="{Binding SumOfCalls}" Header="Sum of Calls" Width="120" ElementStyle="{StaticResource CenteredText}"/>
                <DataGridTextColumn Binding="{Binding SumOfCancellation}" Header="Sum of Cancellation" Width="150" ElementStyle="{StaticResource CenteredText}"/>
                <DataGridTextColumn Binding="{Binding SumOfExpiredCalls}" Header="Sum of Expired Calls" Width="150" ElementStyle="{StaticResource CenteredText}"/>-->

                <!-- עמודת מחיקה בסוף השורה -->
                <DataGridTemplateColumn Width="50">
                    <DataGridTemplateColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </Style>
                    </DataGridTemplateColumn.HeaderStyle>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="DeleteButton_Click"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="4"
                                    FontSize="16"
                                    ToolTip="Delete Volunteer"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center">
                                <TextBlock Text="🗑" />
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <!-- כפתור הוספה -->
        <Button Grid.Row="3" Content="➕ Add Volunteer" Click="btnAdd_Click"
                Style="{StaticResource RoundButtonStyle}"
                HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>
<!--<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Volunteer List" Height="640" Width="900"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F3F6FD" Offset="0.0"/>
            <GradientStop Color="#FDE4EC" Offset="1.0"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <local:VolunteerSortFieldCollection x:Key="VolunteerSortFieldCollection"/>

        --><!-- סגנון כפתור עגול --><!--
    <Style x:Key="RoundButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#5C6BC0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    --><!-- סגנון DataGrid --><!--
    <Style TargetType="DataGrid">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="RowBackground" Value="White"/>
        <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="BorderBrush" Value="#BDBDBD"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
    </Style>

    --><!-- סגנון כותרות ממורכזות --><!--
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="#3F51B5"/>
    </Style>

    --><!-- עיצוב טקסט ממורכז --><!--
    <Style x:Key="CenteredText" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>
    </Window.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        --><!-- Define Rows --><!--
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="10,10,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                --><!-- Sorting --><!--
                <Label Grid.Row="0" Grid.Column="0" Content="Sort By:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox Grid.Row="0" Height="25"  Margin="111,7,447,4"
              SelectedValue="{Binding SelectedSortField, Mode=TwoWay}"
              ItemsSource="{Binding Source={StaticResource VolunteerSortFieldCollection}}" Grid.ColumnSpan="2">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="12"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                --><!-- Filtering --><!--
                <Label Grid.Row="1" Grid.Column="0" Content="Filter by Call Type:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Height="25" Width="180" Margin="37,5,447,6"
              SelectedValue="{Binding SelectedCallType, Mode=TwoWay}"
              ItemsSource="{Binding CallTypeList}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="12"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
            --><!-- DataGrid control (instead of ListView) --><!--
            <DataGrid Grid.Row="1" Margin="10"
                  ItemsSource="{Binding Path=VolunteerList}"/>
            --><!-- כותרת --><!--
            <TextBlock Grid.Row="0" Text="&#x1F3B5; Volunteer List for Music &amp; Singing &#x1F3B5;"
           FontSize="24" FontWeight="Bold" Foreground="#3F51B5"
           HorizontalAlignment="Center" Margin="0,10"/>

            --><!-- מיון וסינון --><!--
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <StackPanel Orientation="Vertical" Margin="10">
                    <TextBlock Text="Sort By:" FontSize="12" Margin="2"/>
                    <ComboBox Width="150" Height="28"
                          SelectedValue="{Binding SelectedSortField, Mode=TwoWay}"
                          ItemsSource="{Binding Source={StaticResource VolunteerSortFieldCollection}}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontSize="12"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Vertical" Margin="10">
                    <TextBlock Text="Filter by Call Type:" FontSize="12" Margin="2"/>
                    <ComboBox Width="150" Height="28"
                          SelectedValue="{Binding SelectedCallType, Mode=TwoWay}"
                          ItemsSource="{Binding CallTypeList}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontSize="12"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </StackPanel>

            --><!-- טבלת מתנדבים --><!--
            <DataGrid Grid.Row="2" Margin="0"
                  ItemsSource="{Binding VolunteerList}"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  EnableRowVirtualization="True"
                  RowDetailsVisibilityMode="VisibleWhenSelected"
                  SelectedItem="{Binding SelectedVolunteer}"
                    MouseDoubleClick="lsvVolunteerList_MouseDoubleClick"
                  HorizontalAlignment="Center">

                <DataGrid.Columns>
                    --><!-- עמודת Id --><!--
                    <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="100" ElementStyle="{StaticResource CenteredText}"/>

                    --><!-- עמודת שם עם הדגשה --><!--
                    <DataGridTemplateColumn Header="Name" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="DarkBlue" TextAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Actions" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" Click="DeleteButton_Click"
                    Background="Tomato" Foreground="White"
                    Padding="5,2" Margin="5,0"/>
                                <CheckBox IsChecked="{Binding Active}" IsEnabled="False" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    --><!-- עמודת תיבת סימון עבור IsActive --><!--
                    <DataGridTemplateColumn Header="Is Active" Width="Auto">
                        <DataGridTextColumn Binding="{Binding TotalResponsesHandled}" Header="Total Responses Handled" Width="150" ElementStyle="{StaticResource CenteredText}"/>
                        <DataGridTextColumn Binding="{Binding TotalResponsesCancelled}" Header="Total Responses Cancelled" Width="150" ElementStyle="{StaticResource CenteredText}"/>
                        <DataGridTextColumn Binding="{Binding TotalExpiredResponses}" Header="Total Expired Responses" Width="150" ElementStyle="{StaticResource CenteredText}"/>
                        --><!--<DataGridTextColumn Binding="{Binding SumOfCalls}" Header="Sum of Calls" Width="120" ElementStyle="{StaticResource CenteredText}"/>
                <DataGridTextColumn Binding="{Binding SumOfCancellation}" Header="Sum of Cancellation" Width="150" ElementStyle="{StaticResource CenteredText}"/>
                <DataGridTextColumn Binding="{Binding SumOfExpiredCalls}" Header="Sum of Expired Calls" Width="150" ElementStyle="{StaticResource CenteredText}"/>-->

                        <!-- עמודת מחיקה בסוף השורה --><!--
                        <DataGridTemplateColumn Width="50">
                            <DataGridTemplateColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>
                            </DataGridTemplateColumn.HeaderStyle>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Active}" IsEnabled="False" HorizontalAlignment="Center"/>
                                    <Button Click="DeleteButton_Click"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="4"
                                    FontSize="16"
                                    ToolTip="Delete Volunteer"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center">
                                        <TextBlock Text="🗑" />
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        --><!-- עמודת TotalResponsesHandled --><!--
                        <DataGridTextColumn Binding="{Binding TotalResponsesHandled}" Header="Total Responses Handled" Width="Auto"/>

                        --><!-- עמודת TotalResponsesCancelled --><!--
                        <DataGridTextColumn Binding="{Binding TotalResponsesCancelled}" Header="Total Responses Cancelled" Width="Auto"/>

                        --><!-- עמודת TotalExpiredResponses --><!--
                        <DataGridTextColumn Binding="{Binding TotalExpiredResponses}" Header="Total Expired Responses" Width="Auto"/>

                        --><!-- עמודת SumOfCalls --><!--
                        <DataGridTextColumn Binding="{Binding SumOfCalls}" Header="Sum of Calls" Width="Auto"/>

                        --><!-- עמודת SumOfCancellation --><!--
                        <DataGridTextColumn Binding="{Binding SumOfCancellation}" Header="Sum of Cancellation" Width="Auto"/>

                        --><!-- עמודת SumOfExpiredCalls --><!--
                        <DataGridTextColumn Binding="{Binding SumOfExpiredCalls}" Header="Sum of Expired Calls" Width="Auto"/>

                </DataGrid.Columns>
            </DataGrid>

            --><!-- כפתור הוספה --><!--
            <Button Grid.Row="2" Content="Add" HorizontalAlignment="Center" Margin="10" Width="150" Height="40" Click="btnAdd_Click"/>
            <Button Grid.Row="3" Content="➕ Add Volunteer" Click="btnAdd_Click"
                Style="{StaticResource RoundButtonStyle}"
                HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
          <Window/>-->