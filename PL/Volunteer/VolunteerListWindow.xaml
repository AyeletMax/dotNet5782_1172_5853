<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Window.Resources>
        <!-- הגדרת המשאב של רשימת הערכים של ה-Enum -->
        <local:VolunteerSortFieldCollection x:Key="VolunteerSortFieldCollection"/>
    </Window.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <!-- הגדרת שורות -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- גריד פנימי עליון - שורת סינון -->
        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Label בעמודה 0 -->
            <Label Grid.Column="0" Content="Select:" VerticalAlignment="Center" Margin="10"/>

            <!-- ComboBox בעמודה 1 -->
            <ComboBox Grid.Column="1" HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch" Margin="10" 
                      SelectedValue="{Binding SelectedSortField, Mode=TwoWay}"
                      ItemsSource="{Binding Source={StaticResource VolunteerSortFieldCollection}}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <!-- פקד הרשימה המתקדמת (ListView) -->
        <ListView Grid.Row="1" Margin="10" ItemsSource="{Binding Path=VolunteerList}" />
        <Button Grid.Row="2" Content="Add" HorizontalAlignment="Center" Margin="10" Width="150" Height="40"/>
    </Grid>
</Window>
