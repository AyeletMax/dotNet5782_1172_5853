<Window x:Class="PL.Call.CallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:PL.Converters"
        Title="{Binding WindowTitle}" Height="700" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.Resources>
        <converters:ConvertIdToVisibility x:Key="ConvertIdToVisibility"/>
        <converters:ConvertIdToReadOnly x:Key="ConvertIdToReadOnly"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Call ID - Only visible in edit mode -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10" 
                        Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="Call ID:" Width="150" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBox Width="300" Height="30" Text="{Binding Call.Id}" IsReadOnly="True"/>
            </StackPanel>

            <!-- Call Type -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Call Type:" Width="150" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <ComboBox Width="300" Height="30"
                ItemsSource="{Binding AvailableCallTypes}"
                SelectedItem="{Binding SelectedCallType, Mode=TwoWay}"
               IsEnabled="{Binding CanEditAllFields}"/>
            </StackPanel>

            <!-- Address -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Address:" Width="150" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBox Width="300" Height="30" Text="{Binding Call.Address, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding CannotEditAllFields}"/>
            </StackPanel>

            <!-- Verbal Description -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Verbal Description:" Width="150" FontWeight="SemiBold" VerticalAlignment="Top"/>
                <TextBox Width="300" Height="80" Text="{Binding Call.VerbalDescription, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" MaxLength="500" IsReadOnly="{Binding CannotEditAllFields}"/>
            </StackPanel>

            <!-- Max Finish Time -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Max Finish Time:" Width="150" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <DatePicker Width="140" SelectedDate="{Binding MaxFinishDate}" IsEnabled="{Binding CanEditMaxTime}"/>
                <TextBox Width="100" Text="{Binding MaxFinishTime, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding CannotEditMaxTime}" Margin="10,0,0,0"/>
            </StackPanel>

            <!-- Call Status -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Status:" Width="150" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBox Width="300" Text="{Binding Call.MyStatus}" IsReadOnly="True"/>
            </StackPanel>

            <!-- Open Time -->
            <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Open Time:" Width="150" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBox Width="300" Text="{Binding Call.OpenTime, StringFormat='dd/MM/yyyy HH:mm:ss'}" IsReadOnly="True"/>
            </StackPanel>

            <!-- Assignments -->
            <GroupBox Grid.Row="7" Header="Assignments" Visibility="{Binding HasAssignments, Converter={StaticResource BooleanToVisibilityConverter}}">
                <DataGrid ItemsSource="{Binding Call.CallAssignments}" AutoGenerateColumns="False" IsReadOnly="True" MaxHeight="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Volunteer ID" Binding="{Binding VolunteerId}" Width="100"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                        <DataGridTextColumn Header="Start" Binding="{Binding EntranceTime, StringFormat='dd/MM/yyyy HH:mm'}" Width="120"/>
                        <DataGridTextColumn Header="End" Binding="{Binding ExitTime, StringFormat='dd/MM/yyyy HH:mm'}" Width="120"/>
                        <DataGridTextColumn Header="Finish Type" Binding="{Binding FinishCallType}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- Edit Permission Info -->
            <TextBlock Grid.Row="8" HorizontalAlignment="Center" Margin="0,10">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CanEditAllFields}" Value="True">
                                <Setter Property="Text" Value="All fields can be edited"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding CanEditOnlyMaxTime}" Value="True">
                                <Setter Property="Text" Value="Only maximum finish time can be edited"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding CannotEdit}" Value="True">
                                <Setter Property="Text" Value="Call is closed or expired - editing not allowed"/>
                                <Setter Property="Foreground" Value="Red"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Buttons -->
            <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="{Binding ButtonText}" Width="120" Height="40" Click="AddButton_Click" IsEnabled="{Binding CanEdit}"/>
                <Button Content="Cancel" Width="120" Height="40" Margin="20,0,0,0" Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>

